# docker compose 인터폴레이션용 (deploy/.env 로 복사해 값 채우기)
# 이미지 태그
LIVE_TAG=live
DEV_TAG=dev

# MySQL 부트스트랩 (최초 기동 시 init 스크립트가 사용)
MYSQL_ROOT_PASSWORD=
APP_DB_USER=nextroom
APP_DB_PASSWORD=
# 주의: APP_DB_USER/APP_DB_PASSWORD 는 .env.live / .env.dev 의 DB_USERNAME/DB_PASSWORD 와 일치해야 함
